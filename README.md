# goalseek
Wrapper from The Goal Seek Excel function (often referred to as What-if-Analysis) is a method of solving for a desired output by changing an assumption that drives it.  The function essentially uses a trial and error approach to back-solving the problem by plugging in guesses until it arrives at the answer
